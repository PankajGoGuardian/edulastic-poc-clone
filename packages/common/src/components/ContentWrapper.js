import styled from 'styled-components';

const ContentWrapper = styled.div`
  width: 100%;
  position: absolute;
  height: 100%;
  overflow: inherit;
  left: 0;
  right: 0;
`;

export default ContentWrapper;
