// import React from 'react';
import styled from "styled-components";

const SubLabel = styled.div`
  color: ${({ theme }) => "#8ed863" || theme.themeColor};
  padding-left: 0.7rem;
  margin-right: 1rem;
`;

export default SubLabel;
