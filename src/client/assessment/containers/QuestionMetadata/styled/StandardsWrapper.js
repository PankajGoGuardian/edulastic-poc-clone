import styled from "styled-components";
import { white, darkGrey1 } from "@edulastic/colors";

const StandardsWrapper = styled.div`
  display: flex;
  color: ${darkGrey1};
  padding: 5px 10px;
  margin: 5px 0px;
`;

export default StandardsWrapper;
