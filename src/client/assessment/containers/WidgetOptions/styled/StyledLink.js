import styled from 'styled-components'
import { themeColor } from '@edulastic/colors'

export const StyledToggleLink = styled.div`
  font-size: 13px;
  font-weight: 600;
  color: ${themeColor};
  border: none;
  background: transparent;
  cursor: pointer;
  text-transform: uppercase;
  height: 30px;
`
export const StyledResourceLink = styled.div`
  color: ${themeColor};
  cursor: pointer;
  font-weight: 500;
  text-transform: uppercase;
  margin-bottom: 15px;
`
