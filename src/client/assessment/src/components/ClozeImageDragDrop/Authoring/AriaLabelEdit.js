import styled from 'styled-components';

const AriaLabelEdit = styled.div`
  height: 40px;
  display: flex;

  & span {
    background: darkgray;
    padding: 4px;
  }
`;

export default AriaLabelEdit;
