import styled from "styled-components";

export const EllipseComp = styled.ellipse`
  position: relative;
  z-index: 10;
  cursor: -webkit-grab;
`;

export const PathComp = styled.path`
  position: relative;
  z-index: 0;
`;
