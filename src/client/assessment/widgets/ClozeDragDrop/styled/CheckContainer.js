import styled from "styled-components";

export const CheckContainer = styled.div`
  white-space: nowrap;
  margin-left: 16px;
  margin-top: 14px;

  .ant-checkbox-wrapper + span,
  .ant-checkbox + span {
    padding: 0px 8px;
  }
`;
