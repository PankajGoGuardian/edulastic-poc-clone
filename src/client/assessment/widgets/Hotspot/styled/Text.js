import styled from 'styled-components';
import { white } from '@edulastic/colors';

export const Text = styled.text`s
  z-index: 6;
  cursor: inherit;
  font-family: sans-serif;
  fill: ${white};
  font-weight: 600 !important;
  font-size: 14px;
  opacity: 1;
  text-anchor: start;
  user-select: none;
`;
