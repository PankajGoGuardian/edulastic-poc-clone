import styled from 'styled-components'
import { FlexContainer } from '@edulastic/common'

export const ExpectAnswer = styled(FlexContainer)`
  justify-content: flex-start;
  margin-bottom: 20px;
  align-items: flex-end;
  max-width: 100%;
`
