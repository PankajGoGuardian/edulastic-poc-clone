import { Pagination } from 'antd'
import styled from 'styled-components'

export const PassagePagination = styled(Pagination)`
  &.ant-pagination {
    justify-content: center;
    margin-top: 12px;
  }
`
