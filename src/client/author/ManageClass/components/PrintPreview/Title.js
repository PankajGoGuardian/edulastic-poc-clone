import React from "react";
import { StyledTitle, Color } from "./styled";

const Title = props => (
  <StyledTitle {...props}>
    <b>
      <Color>Edu</Color>
    </b>
    lastic
  </StyledTitle>
);

export default Title;
