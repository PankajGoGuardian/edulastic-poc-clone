import { connect } from 'react-redux'
import React from 'react'
import { RosterHistoryWrapper } from './styled'

const RosterHistory = () => {
  return <RosterHistoryWrapper>lastAttemptedTable</RosterHistoryWrapper>
}

export default connect(() => ({}), {})(RosterHistory)
