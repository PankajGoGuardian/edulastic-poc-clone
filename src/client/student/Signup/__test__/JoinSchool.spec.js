import { render, cleanup, fireEvent } from '@testing-library/react'

describe('Testing the Join School Modal', () => {
  afterEach(() => {
    cleanup()
  })

  it('> should verify the Join school component visibility', () => {})
  it('> should have default element without banner when no district id is present', () => {})
  it('> should be able to create a home school', () => {})
  it('> should be able to request a new school', () => {})
  it('> should be able to select a school from dropdown', () => {})
  it('> should verify that the selected school appear on the search bar', () => {})
  it('> should display error msg when no school is selected', () => {})
})
