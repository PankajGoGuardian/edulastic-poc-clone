import { render, cleanup, fireEvent } from '@testing-library/react'

describe('Testing the Subject Grade Modal', () => {
  afterEach(() => {
    cleanup()
  })

  it('> verify the subject grade component visibility', () => {})
  it('> should have default form fields without banner', () => {})
  it('> should display error message when tried to submit with grade/subject', () => {})
  it('> verify that the standard and curriculum fields are resetted when school is changed', () => {})
  it('> verify curriculum field is disabled when standard is not select', () => {})
  it('> verify the action triggered when clicked on get started button', () => {})
})
